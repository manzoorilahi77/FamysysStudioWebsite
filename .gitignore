# dependencies
node_modules/

# next.js
.next/
out/
# server logs, when npm run dev / npm start is started detached
dev.log
start.log

# production
build/

# env
.env
.env*.local

# misc
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# testing / coverage
coverage/

# visual-regression run artifacts, regenerated by scripts/verify/vr-*.mjs.
# The scripts write their screenshots into a shots* directory in whatever
# directory they are run from, which is the repository root.
vr-out.txt
shots*/

# scratch space for scripts/deploy.mjs — the packed release, deleted after every run
.deploy-tmp/
